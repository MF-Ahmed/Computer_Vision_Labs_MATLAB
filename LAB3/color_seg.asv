 
function seg_img = color_seg(x,y,imag_1)


  [rr,cc]size(imag_1)
  seg_img=zeros(rr,cc);
    
    for i =1:rr
        for j=1:cc
            if( (hsv_img1(i,j,1)> minthresh) & (hsv_img1(i,j,1)< maxthresh) )
              seg_img(i,j)= 1;
            
            end
                
            end
    end
    
    
end